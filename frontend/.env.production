# Same-origin API when the backend serves this frontend.
# If the API is a different Render service, set this in the Render dashboard
# and rebuild, e.g. https://your-api.onrender.com/api
VITE_API_URL=/api
